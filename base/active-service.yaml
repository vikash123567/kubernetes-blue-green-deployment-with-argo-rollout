kind: Service
apiVersion: v1
metadata:
  name: rollout-bluegreen-active11
spec:
  selector:
    app: rollout-bluegreen
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
  type: LoadBalancer
